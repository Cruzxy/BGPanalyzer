{
  "name": "bgp-analyzer",
  "version": "3.0.0",
  "description": "BGP Analyzer with Node API, MongoDB persistence, and served UI.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check script.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "helmet": "^7.2.0",
    "mongodb": "^6.8.0",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
