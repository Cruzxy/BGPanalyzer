PORT=80
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=bgp_analyzer

# Keeps the API usable on machines without MongoDB installed.
# Set to false in production if MongoDB must be mandatory.
FILE_DB_FALLBACK=true
DATA_FILE=./data/bgp-analyzer.dev.json
